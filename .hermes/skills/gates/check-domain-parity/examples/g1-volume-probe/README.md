# G1 volume probe (tooling smoke only)

Relocated from scaffold `src/` (tidy-up 2026-08-12, Architect CONCUR).

Not an acceptance operand (AR-3.6). Gate / PIT dry-run plumbing may copy or
reference these sources under `G1_OPERAND=tooling_smoke`. Template app `src/`
stays pure product code.
