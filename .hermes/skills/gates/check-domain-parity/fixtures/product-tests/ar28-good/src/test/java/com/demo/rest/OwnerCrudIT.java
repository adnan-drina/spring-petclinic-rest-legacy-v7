package com.demo.rest;

/**
 * Fixture family markers — AR28:crud AR28:db.
 *
 * Named example (CRUD + seeded owner), not dest product. Specimen names such
 * as PetClinic Franklin are not required.
 */
public class OwnerCrudIT {
    void listAndSeededOwner() {
        // GET /api/owners + seeded named owner
    }
}
