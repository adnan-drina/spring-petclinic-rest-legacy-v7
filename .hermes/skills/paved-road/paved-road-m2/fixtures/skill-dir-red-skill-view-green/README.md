# skill-dir-red-skill-view-green

A `$` line under a planning skill's scripts/ exited 1 (a selftest), but the mandated skill step is the skill_view, which is present. Kernel/native needles are script basenames; this must PASS.
