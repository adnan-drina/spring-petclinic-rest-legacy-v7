# not-activated

Transitional state: the activation gate refused and the worker `kanban_block`ed. The audit REFUSES (unmatched [exit 1] on the gate, silence on every later step). That is the correct outcome while pins.planner.activation is not-activated: the card is blocked, not reviewed.
