# Runtime for this project, applied by the shared pipeline's deploy task
# after every green build (the task also pins the image to the platform's
# resolved registry, so the default here is only a bootstrap value).
apiVersion: apps/v1
kind: Deployment
metadata:
  name: spring-petclinic-rest-legacy-v7
  labels:
    app.kubernetes.io/name: spring-petclinic-rest-legacy-v7
    app.kubernetes.io/part-of: spring-petclinic-rest-legacy-v7
    app.openshift.io/runtime: quarkus
    backstage.io/kubernetes-id: spring-petclinic-rest-legacy-v7
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: spring-petclinic-rest-legacy-v7
  template:
    metadata:
      labels:
        app.kubernetes.io/name: spring-petclinic-rest-legacy-v7
        app.kubernetes.io/part-of: spring-petclinic-rest-legacy-v7
        backstage.io/kubernetes-id: spring-petclinic-rest-legacy-v7
    spec:
      imagePullSecrets:
        - name: quay-pull-secret
      containers:
        - name: spring-petclinic-rest-legacy-v7
          # Bootstrap tag only — the deploy-app task pins each PipelineRun's
          # immutable revision tag and sets IfNotPresent (V6 P4.1).
          image: quay.io/rhoai3-coding-demo/spring-petclinic-rest-legacy-v7:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /q/health/live
              port: http
            initialDelaySeconds: 20
            periodSeconds: 20
            timeoutSeconds: 3
          readinessProbe:
            httpGet:
              path: /q/health/ready
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
---
apiVersion: v1
kind: Service
metadata:
  name: spring-petclinic-rest-legacy-v7
  labels:
    app.kubernetes.io/name: spring-petclinic-rest-legacy-v7
    backstage.io/kubernetes-id: spring-petclinic-rest-legacy-v7
spec:
  selector:
    app.kubernetes.io/name: spring-petclinic-rest-legacy-v7
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: spring-petclinic-rest-legacy-v7
  labels:
    app.kubernetes.io/name: spring-petclinic-rest-legacy-v7
    backstage.io/kubernetes-id: spring-petclinic-rest-legacy-v7
spec:
  to:
    kind: Service
    name: spring-petclinic-rest-legacy-v7
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
